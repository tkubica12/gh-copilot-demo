[project]
name = "copilot-token-lab"
version = "0.1.0"
description = "Cross-platform GitHub Copilot token-efficiency measurement harness."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.9.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
