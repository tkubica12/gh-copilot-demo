[project]
name = "data"
version = "0.1.0"
description = "Toy profile data management scripts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
